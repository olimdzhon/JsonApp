mutation MedicPostSaveMutation($medicPost: MedicPostSaveArgs) {
    medicPost: medicPostSave(medicPost: $medicPost) {
        id
        name
        text
        tags {
            id
            name
            conceptId
            conceptType
        }
    }
}
