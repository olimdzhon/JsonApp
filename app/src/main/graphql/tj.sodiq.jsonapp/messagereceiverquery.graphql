query MessageReceiverQuery($id: Int!) {
    user(id: $id) {
        id
        firstName
        secondName
        __typename
        identicon
        profileType
        gender
        active
        photo {
            filename
        }
    }
}